@model IndexViewModel
@{
    ViewData["Title"] = "Manage your account";
}

<h2>@ViewData["Title"].</h2>
<p class="text-success">@ViewData["StatusMessage"]</p>

<div class="container">
    <div class="row">
        <div class="col-md-8 col-md-offset-2">
            <div class="text-center">
                <h1>Manage Your Account</h1>

                @if (Model.HasPassword)
                {
                    <a asp-controller="Manage" asp-action="ChangePassword" class="btn btn-default">Change Password</a>
                }
                else
                {
                    <a asp-controller="Manage" asp-action="SetPassword" class="btn btn-default">Create</a>
                }

                @if (Model.Logins.Count > 0)
                {
                    <a asp-controller="Manage" asp-action="ManageLogins" class="btn btn-default">Manage External Logins</a>
                }

                <a asp-controller="Thoguhts" asp-action="Manage" class="btn btn-default">Manage Thoughts</a>
            </div>
        </div>
    </div>
</div>
